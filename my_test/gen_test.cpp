#include <bits/stdc++.h>
using namespace std;

const vector <char> exprs = {'+','-','*','/'};

int main(int argc, char* argv[]) {
  int size = atoi(argv[1]);

  for (int i = 0 ; i < size; i++) {
    printf("42;\n");
  }
}